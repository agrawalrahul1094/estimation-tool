.header
    height 100%
    display flex
    align-items center
    justify-content space-between
    .logo-container
        height 100%
        img
            height 100%
    .profile-menu
        position: relative;
        z-index: 2;
    .mat-mini-fab
        background-color transparent
        color white
