.header
    height 100%
    display flex
    align-items center
    justify-content flex-end
    .profile-menu
        position: relative;
        z-index: 2;
    .mat-mini-fab
        background-color transparent
        color white
